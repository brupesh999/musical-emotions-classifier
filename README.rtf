INSTALLATION STEPS:

The following modules are required:

pandas
os
matplotlib
numpy
sklearn
librosa
datetime
IPython
warnings
pickle
argparse
time

Running the application:

python3.11 name.py -a f

Different arguments to -a can be -l (just load) and -r (just perform classification); can specify method of classification.
